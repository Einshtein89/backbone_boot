
<div>
    <ul class="contactPerPage">
        Contacts per Page:
        <select name="contactsPerPage" class="form-control" id="contactsPerPageDropdown"
                style="width: 70px; display: inline">
            {#contactsPerPage}
            <option value="{.}" name="contact">
            {.}
            </option>
            {/contactsPerPage}
        </select>
    </ul>
</div>

